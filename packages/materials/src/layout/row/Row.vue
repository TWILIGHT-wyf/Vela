<script lang="ts">
import { vRow } from '@vela/ui'
import { defineMaterial } from '../../utils'

export default defineMaterial(vRow, {
  name: 'Row',
  connectEvent: true,
  fillContainer: true,
})
</script>
